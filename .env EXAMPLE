# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.
POSTGRES_USER=POSTGRES_USER
POSTGRES_PASSWORD=POSTGRES_PASSWORD
POSTGRES_DB=POSTGRES_DB
DATABASE_URL="postgresql://{POSTGRES_USER}:{POSTGRES_PASSWORD}@{localhost or docker image}:5400/{POSTGRES_DB}"
EMAIL_USER="example@gmail.com"
EMAIL_PASS="1111 1111 1111 1111"
BASE_URL="http://localhost:3000"
PORT=3000
GHP_TOKEN="ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
SCANNER_INTERVAL=60000 #milliseconds
SCANNER_REPOS_CHUNK_SIZE=5 #number of repos to scan in one async operation
EMAIL_SENDING_CHUNK_SIZE=5 #number of emails to send in one async operation
DOCKER_IMAGE=false
API_KEY="YOUR_API_KEY"
API_USE_AUTH=false